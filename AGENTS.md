# AGENTS.md 项目行为规则

## 输出风格

1. 回答优先给出结论。
2. 代码优先给出完整、可运行的代码片段，拒绝残缺伪代码。
3. 出现报错时，先定位根因，再给出修复方案。
4. 修改代码后，说明修改了什么以及为什么。
5. 尽量使用结构化输出：分点、表格、代码块。
6. 不要多余客套、不要过度夸赞，保持技术务实风格。

## 编码规范

1. 遵循项目已有代码风格，不要私自全局格式化文件。
2. 修改代码时只改与当前问题相关的部分，尽量保持最小改动。
3. 重要逻辑增加简短注释，不写冗余注释。
4. 遇到类型、边界、异常情况必须考虑。
5. 不要为了“代码更优雅”而擅自进行无关重构。
6. 优先复用项目已有的工具、组件、函数和依赖。

## 项目分析

开始修改前必须先了解现有项目：

1. 查看项目目录结构。
2. 查看与当前任务相关的源码。
3. 查看相关配置文件和依赖配置。
4. 确认项目使用的语言、框架、版本和现有代码风格。
5. 基于实际项目代码制定修改方案。
6. 禁止脑补不存在的文件、接口、配置或项目结构。

## 变更范围

1. 只修改完成当前任务所必需的文件和代码。
2. 禁止无关重构、目录重组、代码风格迁移和 API 替换。
3. 发现其他潜在问题时，可以指出，但不要擅自修改。
4. 如果解决当前问题必须进行较大范围修改，应先向用户说明原因和影响。
5. 涉及删除文件、破坏性修改或可能导致数据丢失的操作，必须先向用户确认。

## 依赖管理

1. 不得自动安装、升级、降级或删除依赖。
2. 不自动安装全局依赖。
3. 如果解决问题需要新增或修改依赖，只修改依赖配置，并明确告诉用户需要执行的安装命令。
4. 不擅自升级项目现有依赖版本。
5. 用户没有明确要求时，不执行依赖安装。

## 执行行为

默认情况下：

1. 只负责修改源码和配置文本文件。
2. 禁止主动执行测试、构建、运行程序。
3. 禁止主动下载或安装依赖。
4. 测试、编译、运行、安装依赖交给用户手动执行。

只有用户明确使用以下意图时，才允许执行对应操作：

- “测试” → 可以执行测试
- “运行” → 可以运行程序
- “构建” → 可以执行构建
- “执行” → 可以执行用户明确要求的命令
- “安装依赖” → 可以安装依赖

如果用户没有明确要求，不得因为“验证修改是否正确”而主动运行测试或构建。

## Git

1. 修改前先检查 git status，了解工作区已有修改。

2. 禁止覆盖、撤销或删除用户已有的未提交修改。

3. 修改完成后，如果本次任务已经完成，执行 git add + git commit。

4. 只提交本次任务涉及的文件，禁止把其他已有修改一起提交。

5. 使用简洁、语义化的 commit message。

6. 不推送远程仓库。

7. 禁止使用可能造成数据丢失的操作，例如：
   
   - git reset --hard
   - git clean -fd
   - git checkout -- <file>
   - git restore <file>
   
   除非用户明确要求。

## 排错

1. 拿到报错日志时，优先读取和分析原始日志。
2. 不要根据错误现象直接主观猜测根因。
3. 优先定位：
   - 实际报错位置
   - 第一处异常
   - 调用链
   - 配置问题
   - 依赖/版本问题
   - 类型或运行时问题
4. 如果信息不足以确定根因，明确指出缺少什么信息，并向用户询问。
5. 不要编造不存在的参数、接口、配置项或错误原因。
6. 修复后优先给出完整、可直接替换的代码。
7. 如果需要用户执行命令，给出可以直接复制执行的完整命令。

## 输出修改结果

完成修改后简要说明：

1. 修改了哪些文件。
2. 修改了什么。
3. 修改原因。
4. 是否修改依赖。
5. 是否执行了测试/构建/运行。
6. Git commit 是否已创建。
